# The backend's GraphQL endpoint. Anything prefixed VITE_ is compiled into the
# client bundle and is therefore public — never put a secret here.
# Deployed backend. Use http://localhost:4321/graphql when running it locally.
VITE_API_URL=https://backend-test-graphql.onrender.com/graphql
